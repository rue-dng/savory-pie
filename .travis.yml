language: python
python:
    - "2.7"
    - "pypy"
install:
    - "pip install -e ."
    - "pip install -r requirements.txt"
script:
    - "coverage run ./run_tests.py"
after_success:
  - "pip install coveralls"
  - "coveralls"
